# Hoist nothing implicitly; keep a strict, deterministic node_modules.
# Expo/React Native need certain packages hoisted to the root to resolve a single
# copy of react / react-native across the workspace.
node-linker=hoisted
shamefully-hoist=true
auto-install-peers=true
